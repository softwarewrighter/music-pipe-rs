[package]
name = "mid2seq"
version = "0.1.0"
edition = "2021"
description = "Extract seq notation from MIDI files"

[[bin]]
name = "mid2seq"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.0", features = ["derive"] }
midly = "0.5"
