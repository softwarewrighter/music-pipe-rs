[package]
name = "music-ir"
version = "0.1.0"
edition = "2021"
description = "Shared event types and JSONL I/O for music-pipe-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
