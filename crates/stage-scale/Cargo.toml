[package]
name = "stage-scale"
version = "0.1.0"
edition = "2021"
description = "Constrain pitches to a scale/mode for music-pipe-rs"

[[bin]]
name = "scale"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
music-ir = { path = "../music-ir" }
